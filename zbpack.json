{
    "build_command": "npm install && npm run build && python manage.py collectstatic --noinput && python manage.py migrate --noinput",
    "start_command": "_startup",
    "python": {
        "entry": "config.wsgi:application"
    }
}